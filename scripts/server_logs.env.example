# Copy this to .env (repo root) or pass it via --env-file.
SERVER_SSH_TARGET=user@example.com
# Optional:
# SERVER_SSH_PORT=22
# SERVER_SSH_OPTS=-i ~/.ssh/id_ed25519

# Remote directory that contains your compose.yml / podman-compose setup.
SERVER_COMPOSE_PATH=/opt/pawlia
