# PawLia TODO / Findings
# Laufende Liste — bei neuen Funden ergänzen. Erledigtes nach unten unter DONE verschieben.

## Offen

- [ ] tests/test_llm_factory.py: zwei Tests rot (vorbestehend, NICHT von mir) —
      test_context_size_uses_probe_over_heuristic,
      test_probe_disabled_falls_back_to_heuristic. Betreffen context_probe;
      auf sauberem develop-Stand ebenfalls rot. Prüfen/fixen.

- [ ] config.openai-eval.yaml: enthält API-Keys. Auf develop bereits UNTRACKED
      (Commit 0115805) + gitignored — also nicht mehr im HEAD. ABER: die Keys
      stecken noch in der History (2aefc1e, 4a89b3e, eb330b9, 6e45752), und die
      ist auf origin. "Vollständig raus" = History-Rewrite (git filter-repo) +
      Force-Push + Key-Rotation. Destruktiv → wartet auf explizite Freigabe.

## DONE

- [x] 2026-06-13: Discord `always_thread` implementiert — konversationelle
      Nachrichten in Server-Textkanälen starten automatisch einen Thread (wie
      Matrix); Befehle/DMs bleiben im Kanal. Text-, Bild-, Voice-, Datei-Pfad
      thread-aware. Doku in docs/interfaces.md ergänzt.
- [x] 2026-06-13: pawlia/tools/files_tools.py gelöscht (toter Code, nie
      registriert) + zugehörige Tests (test_files_tools.py, 2 e2e-Skripte).
- [x] 2026-06-13: memory-Skill `search` durchsucht jetzt die VOLLE Log-Historie
      (search_logs in memory.py), nicht nur 2 Tage → auch alte, nicht gedreamte
      Gespräche recallbar; bei null Treffern kein Dump. Bewusster Aufruf, keine
      Auto-Injektion (Wunsch des Users).
- [x] 2026-06-13: Sprach-Bug gefixt — DreamWiki-Backend (Default) gab
      "No relevant information found." englisch zurück, wurde vom Skill nicht als
      leer erkannt und als "## Langzeit-Wissen (Wiki)" durchgereicht. Jetzt
      deutsch + Skill filtert Sentinels sprachunabhängig (_is_empty_rag).

- [x] 2026-06-12: agents.md auf aktuellen Stand gebracht (Sandbox, Attachments,
      Discord/VoIP, Vision-/Context-Probe, Credentials, Skill-Config) — 300e8e0
- [x] 2026-06-12: Discord-Abschnitt in docs/interfaces.md ergänzt — 300e8e0
- [x] 2026-06-12: CHANGELOG [Unreleased] nachgetragen — 300e8e0
- [x] 2026-06-12: .gitignore bereinigt (Duplikat, tote Patterns) — 300e8e0
