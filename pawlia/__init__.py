"""PawLia - LangChain-based conversational AI assistant."""

# Single source of truth for the project version (SemVer, MAJOR.MINOR.PATCH).
# The User-Agent (pawlia.utils.PAWLIA_USER_AGENT) is derived from this — never
# hardcode the version anywhere else. Bump only on release/* or hotfix/* branches
# and tag main as vX.Y.Z. See agents.md › "Versioning & Releases (git-flow)".
__version__ = "0.1.0"
