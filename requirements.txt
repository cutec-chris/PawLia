aiohttp>=3.9
jinja2>=3.0
aiodns>=3.0    
langchain-core>=0.3
pydantic>=2.0
langchain-ollama>=0.3
langchain-openai>=0.3
markdown>=3.5
matrix-nio>=0.25
python-telegram-bot>=20.0
PyYAML>=6.0
# Pure-Python IANA timezone database — required for zoneinfo on slim/Alpine
# images that ship no system tz data (timezone-aware reminders/scheduler).
tzdata>=2024.1
requests>=2.32
networkx>=3.0
beautifulsoup4>=4.12
html2text>=2024.2
ollama>=0.4
trafilatura>=2.0,<3
rank_bm25>=0.2
lxml>=5.4,<7
# Optional Matrix attachment conversion:
# markitdown[pdf,docx,xlsx,xls,pptx]>=0.1
# Disabled in the default install because markitdown pulls magika -> onnxruntime,
# which has no pip wheel for Alpine/musllinux aarch64 on Python 3.14.

discord.py[voice]>=2.7
# VoIP / telephony extras → see requirements-voip.txt
